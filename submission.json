{
    "config": ["config/data-params.json", "config/process_params.json"],  # paths of at least 2 config files
    "build": "run.py", # path of your build script
    "library": ["src"], # directory containing (your) library code
    "notebook": ["notebooks/EDA_Steam_Dataset.ipynb"], # path to at least one notebook meant for reading by someone other than you.
    "targets": ["data", "process"] # at least two targets
}
